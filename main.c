#include "stdlib.h"
#include "stdio.h"
#include "utilityFunctions.h"
#include "stdbool.h"

int main()
{
    int *front = malloc(sizeof(int));
    int *rear = malloc(sizeof(int));
    int *size = malloc(sizeof(int));
    int *curSize = malloc(sizeof(int));
    *size = 1000;
    *curSize = 0;
    *front = -1;
    *rear = -1;
    int array[1000];


    return 0;
}



